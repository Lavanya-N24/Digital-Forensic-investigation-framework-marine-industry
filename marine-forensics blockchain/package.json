{
  "name": "marine-forensics",
  "version": "1.0.0",
  "scripts": {
    "node": "npx hardhat node",
    "deploy": "npx hardhat run --network localhost scripts/deploy.js"
  },
  "devDependencies": {
    "hardhat": "^2.20.1",
    "@nomiclabs/hardhat-ethers": "^2.1.0",
    "ethers": "^5.7.2"
  }
}
